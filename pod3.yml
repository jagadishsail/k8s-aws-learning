apiVersion: v1
kind: Pod
metadata:
  name: pod3-github-javaDocker
  labels:
    app: hello-world
spec:
  containers:
    - name: nginx-jagadish
      image: jagadishsail/java-docker
      ports:
        - name: nodejs-port
          containerPort: 3000
